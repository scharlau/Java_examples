Task 

Create a custom collection class that stores elements with integer value and some flag indicating the element should be hidden or visible. Amend the collection code so when the contains method is called hidden elements are ignored.